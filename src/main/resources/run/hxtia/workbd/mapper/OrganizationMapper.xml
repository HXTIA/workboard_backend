<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="run.hxtia.workbd.mapper.OrganizationMapper">
    <!--
        1、若mybatis-plus不能满足需求
        2、请在这自己手动书写映射文件
        3、按照这个目录结构，会自动扫描
        4、下面是一个简单的例子
     -->

    <!-- 因为直接使用 mp的插入，什么值都没有，会插入失败 -->
    <insert id="insertDefaultRegisterOrg" useGeneratedKeys="true" keyProperty="id" parameterType="Organization">
        INSERT INTO organization (  ) VALUES (  )
    </insert>

</mapper>
